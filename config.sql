create database IF NOT EXISTS login;

use login;

create table IF NOT EXISTS Register(Email varchar(100) NOT NULL,Password varchar(255), PhoneNo varchar(255) NOT NULL,PRIMARY KEY (Email));


create table  students(name varchar(100) NOT NULL,nickname varchar(255), crushname varchar(255),about me varchar(255),PhoneNo varchar(255),birthday date);



